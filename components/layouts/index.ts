/**
 * Layout components for the FastForm application.
 *
 * @example
 * ```tsx
 * import { AppLayout } from '@/components/layouts'
 *
 * export default function MyPage() {
 *   return (
 *     <AppLayout>
 *       <YourContent />
 *     </AppLayout>
 *   )
 * }
 * ```
 */

export { AppLayout, AppNavbar, UserAvatarMenu } from './app-layout'
export type { AppNavbarProps, UserAvatarMenuProps } from './app-layout'
